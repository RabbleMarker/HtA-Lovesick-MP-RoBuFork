
country_event = {
	id = 99999
	title = "You're too OP."
	desc = "Nerfs conquest RP. At first you won't be able to get any but after taking a few you should be able to get some RP."
	picture = "disease"
	
	trigger = {
		OR = {
			tag = PER
			tag = JAP	
			tag = GXI
		}
		civilized = no
		NOT = { has_country_modifier = persia_rp_nerf }
	}
	
	mean_time_to_happen = {
		months = 0
	}
	option = {
		name = "Understandable."
		add_country_modifier = {
			name = persia_rp_nerf
			duration = -1
		}
	}
}
country_event = {
	id = 99969
	title = "Getting rid of your RP Nerf."
	desc = "Since you civved you don't need this anyway."
	picture = "disease"
	
	trigger = {
		OR = {
			tag = PER
			tag = JAP
			tag = GXI
		}
		civilized = yes
		has_country_modifier = persia_rp_nerf
	}
	
	mean_time_to_happen = {
		months = 0
		}
	option = {
		name = "Nice."
		remove_country_modifier = {
			name = persia_rp_nerf
		}
	}

}